//Runs once at the start and sets environment variable
function setup() {
  createCanvas(windowWidth,windowHeight);
  background(0)
}

//Runs ever frame
function draw() {
  fill(255,0,0);
  rect(0, 0, 100, 100);
}